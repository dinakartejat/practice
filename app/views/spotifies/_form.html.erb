<%= form_with(model: spotify) do |form| %>
  <% if spotify.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(spotify.errors.count, "error") %> prohibited this spotify from being saved:</h2>

      <ul>
        <% spotify.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :gmail, style: "display: block" %>
    <%= form.text_field :gmail %>
  </div>

  <div>
    <%= form.label :password, style: "display: block" %>
    <%= form.number_field :password %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
