<%= form_with(model: fb) do |form| %>
  <% if fb.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fb.errors.count, "error") %> prohibited this fb from being saved:</h2>

      <ul>
        <% fb.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :firstname, style: "display: block" %>
    <%= form.text_field :firstname %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
