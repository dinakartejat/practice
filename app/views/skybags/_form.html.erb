<%= form_with(model: skybag) do |form| %>
  <% if skybag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(skybag.errors.count, "error") %> prohibited this skybag from being saved:</h2>

      <ul>
        <% skybag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
