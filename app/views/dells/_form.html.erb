<%= form_with(model: dell) do |form| %>
  <% if dell.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dell.errors.count, "error") %> prohibited this dell from being saved:</h2>

      <ul>
        <% dell.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :ram, style: "display: block" %>
    <%= form.text_field :ram %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
